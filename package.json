{
  "name": "book-illustrator",
  "private": true,
  "version": "0.1.0",
  "description": "Gradion Take-Home Assessment: Book Illustration Studio powered by Gemini API",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "test": "npm run test --workspace backend && npm run test --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "9.2.4"
  }
}
